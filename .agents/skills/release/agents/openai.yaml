interface:
  display_name: "Release"
  short_description: "Version, tag, merge, and refresh a branch"
  default_prompt: "Use $release to bump VERSION, merge the current branch into the default branch, tag the merge commit, and recreate the branch."
